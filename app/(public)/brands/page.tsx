import { InfoPage } from "@/components/catalog/InfoPage";

export const metadata = { title: "Подбор по брендам — АгроЗаказ" };

export default function Page() {
  return <InfoPage title="Подбор по брендам" />;
}
